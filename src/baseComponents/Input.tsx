import styled from "@emotion/styled";

export const Input = styled.input({
  fontSize: "0.85em",
  padding: 2,
  margin: "-2px 0",
  borderWidth: 1,
  borderRadius: 4,
  width: "calc(100% - 4px - 2px);",
});
